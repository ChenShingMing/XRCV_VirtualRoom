pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

include ':launcher'
include ':unityLibrary'
**INCLUDES**

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)

    repositories {
        google()
        mavenCentral()

        // Firebase Unity SDK local m2repository
        maven {
            url new File(settingsDir.toPath().resolve("../../../../../../Assets/GeneratedLocalRepo/Firebase/m2repository").normalize().toString())
        }

        flatDir {
            dirs "${project(':unityLibrary').projectDir}/libs"
        }
    }
}
